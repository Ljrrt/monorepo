# Prerequisites

- [Vercel CLI](https://vercel.com/docs/cli) installed globally

# Develop locally:

```
npm install
vc dev
```

```
open http://localhost:3000
```

# Build locally:

```
npm install
vc build
```

# Deploy:

```
npm install
vc deploy
```
